---
type: Summary
title: "Using media queries (MDN)"
description: "Media queries are used for the following: A media query is composed of an optional media type and any number of media feature expressions, which may optionally be combined in various ways us"
resource: "https://developer.mozilla.org/en-US/docs/Web/CSS/CSS_media_queries/Using_media_queries"
source_file: "Raw/02_architectures/mdn-media-queries-using.md"
tags: [architectures, summary]
timestamp: "2026-07-27T00:00:00Z"
---

# Using media queries (MDN)

Extractive digest of the immutable capture in
`Raw/02_architectures/mdn-media-queries-using.md`
(retrieved 2026-07-27).
Lead text and headings are quoted verbatim from
the source; read the capture for the full text.

Source: <https://developer.mozilla.org/en-US/docs/Web/CSS/CSS_media_queries/Using_media_queries>

## Opening

> Media queries are used for the following:
> A media query is composed of an optional _media type_ and any number of _media feature_ expressions, which may optionally be combined in various ways using _logical operators_. Media queries are case-insensitive.
> The type is optional (assumed to be `all`) except when using the `only` logical operator.
> For example, the [`hover`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/At-rules/@media/hover) feature allows a query to check whether the device supports hovering over elements. Media feature expressions test for their presence or value, and are entirely optional. Each media feature ...

## Contents of the source document

- Using media queries
  - Syntax
  - Targeting media types
  - Targeting media features
  - Creating complex media queries
    - Combining multiple types or features
    - Testing for multiple queries
    - Inverting a query's meaning
    - Improving compatibility with older browsers
    - Testing for multiple features with or
  - See also
  - Help improve MDN
